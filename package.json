{
    "scripts": {
        "format": "prettier --write \"./**/*.*\"",
        "dev": "vuepress dev docs",
        "build": "vuepress build docs"
    },
    "devDependencies": {
        "vuepress": "^1.5.0"
    },
    "prettier": {
        "tabWidth": 4,
        "overrides": [
            {
                "files": "*.md",
                "options": {
                    "tabWidth": 2
                }
            }
        ]
    }
}
